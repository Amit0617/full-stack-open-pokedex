name: Hello World

on:
  push:
    branches:
      - master

jobs:
  hello_world_job
    runs-on: ubuntu-22.04
    steps:
      - name: Say Hello
        run: |
          echo "Hello World!" && shutdown now